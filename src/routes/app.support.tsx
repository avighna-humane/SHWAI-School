import { createFileRoute } from "@tanstack/react-router";
import { SupportWorkspace } from "@/components/v5/support-workspace";
export const Route = createFileRoute("/app/support")({ component: SupportWorkspace });
